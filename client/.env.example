# Clerk Publishable Key — required for authentication
# Get one at https://dashboard.clerk.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend API URL (default: http://localhost:3000)
# Set to empty string '' to use Vite's dev proxy (see vite.config.js)
# VITE_API_URL=http://localhost:3000
